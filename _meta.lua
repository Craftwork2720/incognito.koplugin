local _ = require("gettext")
return {
    name = "incognito",
    fullname = _("Incognito"),
    description = _([[Opens books in incognito mode — no reading history, no progress saved, no document settings written to disk, no reading statistics recorded.]]),
}
